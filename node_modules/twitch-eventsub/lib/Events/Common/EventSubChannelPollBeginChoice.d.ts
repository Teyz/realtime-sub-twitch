/** @private */
export interface EventSubChannelPollBeginChoiceData {
    id: string;
    title: string;
}
/**
 * A choice in a poll, as defined when beginning that poll.
 */
export declare class EventSubChannelPollBeginChoice {
    /** @private */
    protected readonly _data: EventSubChannelPollBeginChoiceData;
    /** @private */
    constructor(data: EventSubChannelPollBeginChoiceData);
    /**
     * The ID of the choice.
     */
    get id(): string;
    /**
     * The title of the choice.
     */
    get title(): string;
}
