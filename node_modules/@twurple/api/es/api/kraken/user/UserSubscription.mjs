import { __decorate } from "tslib";
import { rtfm } from '@twurple/common';
import { Channel } from "../channel/Channel.mjs";
import { Subscription } from "../Subscription.mjs";
/**
 * A relation of a previously given user subscribing to a channel.
 */
let UserSubscription = class UserSubscription extends Subscription {
    /**
     * The subscribed channel.
     */
    get channel() {
        return new Channel(this._data.channel, this._client);
    }
    /**
     * The ID of the subscribed channel.
     */
    get channelId() {
        return this._data.channel._id;
    }
};
UserSubscription = __decorate([
    rtfm('api', 'UserSubscription', 'channelId')
], UserSubscription);
export { UserSubscription };
